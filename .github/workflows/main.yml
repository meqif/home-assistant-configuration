name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: docker://python:3.7
    - name: Install Home Assistant CLI
      run: python3 -m pip install homeassistant
    - name: Check Home Assistant Configuration
      run: hass --script check_config
